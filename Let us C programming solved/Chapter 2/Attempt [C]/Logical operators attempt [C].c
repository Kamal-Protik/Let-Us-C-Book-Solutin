#include<stdio.h>
#include<stdlib.h>

int main()
{
    int a = 10,b = 12,c = 0;
    if(5 && c!=8 || !c)// put (a != 6 && b>5) and (a == 9 || b<3) and (!(a<10)) and (!(a>5 && c)) and (5 && c!=8 || !c) in the if statement for check.
        printf("\nThe value is 1 \n");
    else
        printf("\nThe value is 0 \n");
    return 0;
}

